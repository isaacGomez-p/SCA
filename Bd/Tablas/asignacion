CREATE TABLE tienda.asignacion
(
    idasignacion integer NOT NULL DEFAULT nextval('tienda.asignacion_idasignacion_seq'::regclass),
    sede text COLLATE pg_catalog."default",
    fecha date,
    estado boolean
)
WITH (
    OIDS = FALSE
)
TABLESPACE pg_default;